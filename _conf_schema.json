{
  "server_host": {
    "type": "string",
    "description": "服务器 IP 或域名",
    "hint": "例如 ts.example.com 或 127.0.0.1"
  },
  "server_port": {
    "type": "int",
    "description": "TS3 语音服务器端口",
    "hint": "通常是 9987"
  },
  "serverquery_port": {
    "type": "int",
    "description": "ServerQuery 端口",
    "hint": "通常是 10011，一般保持默认即可",
    "default": 10011
  },
  "serverquery_username": {
    "type": "string",
    "description": "ServerQuery 账号"
  },
  "serverquery_password": {
    "type": "string",
    "description": "ServerQuery 密码",
    "obvious_hint": true
  },
  "enable_plain_text_trigger": {
    "type": "bool",
    "description": "允许不带 / 的纯文本触发",
    "hint": "开启后，直接发送 ts、上号、人呢 也会触发查询",
    "default": false
  },
  "enable_monitor": {
    "type": "bool",
    "description": "开启进入/离开监控",
    "hint": "开启后插件会持续轮询 TS3 在线列表，并向已绑定的会话推送上线/下线通知",
    "default": false
  },
  "monitor_interval_seconds": {
    "type": "int",
    "description": "监控轮询间隔秒数",
    "hint": "默认 5 秒，代码也会强制不低于 5 秒。请勿设置过低，太频繁查询可能会被 TS3 服务器拉入黑名单。",
    "obvious_hint": true,
    "default": 5
  },
  "debug": {
    "type": "bool",
    "description": "输出详细调试日志",
    "hint": "开启后会在 AstrBot 日志中打印更详细的 TS3 查询过程",
    "default": false
  }
}
